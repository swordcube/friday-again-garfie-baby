function onNoteHit(e:NoteHitEvent) {
    if(e.noteType == NOTE_TYPE_ID) // TODO: not require this if statement
        e.playSingAnim = false;
}

function onNoteMiss(e:NoteMissEvent) {
    if(e.noteType == NOTE_TYPE_ID) // TODO: not require this if statement
        e.playMissAnim = false;
}